# NohBoardColor 后续改进计划（待人工评审）

> - 计划版本：Draft 0.1
> - 对应当前版本：v1.4.4
> - 草拟日期：2026-08-10
> - 状态：**窗口辅助功能已获批准并实现，待人工验收；位置预设仍待评审**

## 实现进度 - 2026-08-10

- [x] 主窗口图钉按钮和置顶状态持久化
- [x] `Ctrl+Alt+T` 全局快捷键切换置顶
- [x] `Ctrl+Alt+O` 全局快捷键切换空闲透明
- [x] 鼠标离开后半透明并隐藏标题栏，移回后恢复
- [x] 键盘背景与键帽独立透明度设置，文字保持 100% 不透明
- [x] “仅按下键不透明”和“全部按钮不透明”两种模式
- [x] 修复 Settings 在字体/DPI 缩放下被裁剪的问题
- [ ] 人工体验验收
- [ ] IDE 与键盘位置预设的交互评审和实现

## 1. 目标

让刚开始学习编程和打字的小朋友能够同时观察编程 IDE 与键位提示，并根据当前教学步骤灵活安排两个窗口的位置。改进应保持操作简单，避免动画和颜色信息造成新的认知负担。

## 2. 建议功能

### 2.1 屏幕置顶

- 在设置中增加 `Always on top` 开关，默认关闭。
- 开启后键盘窗口保持在 IDE 上方，但不主动抢夺输入焦点。
- 设置需要持久化，并允许在不重启程序的情况下立即切换。

### 2.2 半透明键盘与激活按键（已实现，待验收）

- 键盘背景和键帽分别设置透明度，范围为 20%–100%，默认值分别为 40% 和 75%。
- 键位文字独立保持 100% 不透明，避免儿童在渐暗过程中看不清按键。
- 当前按住的按键恢复为 100% 不透明，松开后沿用现有渐暗过程，最终回到键盘的基础透明度。
- 通过背景、键帽、文字三个不拦截输入的窗口层实现独立透明度，避免整个 WinForms 窗口统一透明造成文字模糊。

### 2.3 Alpha 通道主题（待评审）

- 支持具有 Alpha 通道的主题资源，明确采用“文字 Alpha 不透明度 > 键帽 > 键盘背景”的视觉层级。
- 当前透明高亮固定使用白底黑字；后续主题可评审按背景亮度自动选择高对比文字和键帽颜色。
- 键盘背景可使用低干扰的卡通图案，但须验证不会遮挡 IDE 代码或分散儿童注意力。
- 主题需要提供纯色回退方案，并评估不同屏幕、深浅色 IDE 与高对比度设置下的可读性。

### 2.4 IDE 与键盘位置切换

- 提供上、下、左、右等少量窗口位置预设，并兼容手动拖动位置。
- 可评审是否增加一个可配置快捷键，在位置预设之间循环切换。
- 保留上次使用的位置；多显示器环境下需要校验窗口仍位于可见区域。
- 第一版不做自动识别或控制 IDE，避免误移动其他程序窗口。

## 3. 建议实施顺序

1. 人工确认交互方式、默认值和设置文案。
2. 实现并独立验收屏幕置顶。
3. 实现键盘基础透明度及激活按键不透明。
4. 验证渐暗效果从 100% 不透明平滑回到基础透明度。
5. 实现位置预设与持久化。
6. 完成多显示器、DPI、长按、组合键和背景图片样式测试。

## 4. 人工评审问题

- Alpha 主题中的卡通背景应采用哪些亮度、对比度与内容限制？
- 激活时只让当前按键不透明，还是让整个键盘临时不透明？建议只突出当前按键。
- 位置切换需要界面按钮、快捷键，还是两者都需要？
- 置顶窗口是否需要可选的鼠标穿透？这可能影响编辑键盘布局，应谨慎决定。
- 是否需要为不同课程保存多套“IDE + 键盘位置”预设？

## 5. 验收标准草案

- 置顶、透明度和位置设置均可保存并即时生效。
- 键盘半透明时，IDE 内容清晰可见；激活按键仍能明显辨认。
- 激活按键从完整高亮平滑回到基础透明度，不出现闪烁或突然跳变。
- 功能关闭后与 v1.4.0 的基础渐暗显示行为一致。
- 不抢夺 IDE 的键盘焦点，不影响正常输入。
- 在常见缩放比例和多显示器环境中不会把窗口保存到屏幕外。

窗口辅助功能已按人工意见完成开发，待实际体验验收后再发布；位置预设仍需确认交互方式后才能实施。
