# 更新说明

本项目从 `v1.3.0` 起继承上游 NohBoard，并使用语义化版本号记录此分支的变化。

## v1.4.4 - 2026-08-11

### 双状态字符显示模式

- `Settings` 增加 `Dual-state key labels`，可在实体键帽式双字符和 NohBoard 原版居中单字符之间切换。
- 实体键帽模式为默认值：空闲的非字母双状态键同时显示左下较小的普通字符和右上较大的 S 字符。
- 实体键帽模式中，键被激活后只显示按下瞬间对应的字符，并保持该字符在键帽上的原位置和字号。
- 每次按键会锁存按下瞬间的 Shift 状态，因此保持和渐暗期间不会因 Shift 后续变化而切换字符。
- A–Z 字母不采用分角排版，始终只显示一个居中文字。
- 原版模式始终只显示一个居中字符，并随当前或保留的 Shift 状态切换。
- 旧配置无需迁移；缺少新字段时自动使用实体键帽模式。
- 实际 `;/:` 输入测试确认：实体键帽模式空闲时显示两个字符，无 Shift 激活时只保留左下 `;`，Shift 激活时只保留右上 `:`；普通与透明窗口一致。原版模式的三个状态均为单一居中文字。
- Settings 字体/DPI 布局探针确认新增选项组、外观组和底部按钮均位于客户区内且互不重叠。
- 提供 Windows x64 自包含绿色压缩包，无需管理员权限或额外安装 .NET Runtime，并附带独立使用说明和全新的默认配置。

## v1.4.3 - 2026-08-10

### Shift S 状态显示

- 移除 v1.4.2 使用的 `↑` 角标，避免尺寸过小时引起误解。
- A–Z 英文字母保留 S 状态时不偏移，只显示居中的大写字母，状态结束后恢复小写。
- 非英文字母双状态键改为实体键帽式排版：普通字符缩小并放在左下，S 字符保持较大并放在右上。
- 普通窗口和透明窗口复用相同文字绘制逻辑，保持显示一致。
- 使用 3000 ms 渐暗实际测试 `Shift+A` 与 `Shift+;`：普通和透明模式在渐暗后半段均显示居中的 `A`，以及右上 `:`、左下较小 `;` 的双字符键帽。

## v1.4.2 - 2026-08-10

### 窗口辅助功能

- 主窗口增加鼠标移入时显示的图钉按钮，可直接切换窗口置顶。
- 右键菜单增加置顶和空闲透明快速开关。
- 增加 `Ctrl+Alt+T` 全局快捷键切换置顶，以及 `Ctrl+Alt+O` 切换空闲透明。
- 设置窗口增加 `Inactive Window Appearance`，键盘背景和键帽透明度可分别在 20%–100% 之间设置。
- `General` 增加 100%–300% 键位字体倍率，默认值为 130%。
- 鼠标离开主窗口后自动隐藏标题栏并使用设定透明度；移回后恢复完整窗口。
- 采用背景、键帽、文字三层窗口绘制，键位文字在空闲透明和高亮渐暗过程中始终保持 100% 不透明。
- 按键激活时可选择仅让物理按住的键通过独立覆盖窗恢复不透明，或让全部键帽恢复不透明。
- 编辑模式、右键菜单和子对话框打开期间强制保持完整窗口，避免影响编辑操作。
- 置顶、透明度和按键激活模式均随全局设置持久化。

### 修复

- 修复 `Settings` 新增区域未正确参与字体/DPI 自动缩放，导致原有分组重叠、右侧内容及底部按钮被裁剪的问题。
- 修复按键高亮渐暗时文字透明度同步降低、影响儿童辨认键位的问题。
- 修复隐藏标题栏时键盘客户区向左上移动的问题；现在切换前后保持相同屏幕坐标。
- 修复透明文字与洋红透明键发生抗锯齿混色的问题；透明状态文字固定为黑色。
- 修复透明状态按下键和渐暗键帽与洋红透明键混色的问题；高亮固定使用白色键帽和黑色文字。
- 修复普通窗口中保留的 S 字符与缓存的非 S 字符重叠、渐暗时非 S 字符逐渐显现的问题；现在与透明窗口一致，只显示 S 字符直到该键完全熄灭。
- 修复最小化期间仍切换无边框样式、导致窗口无法从任务栏恢复的问题。
- 修复右键菜单状态文字与快捷键显示过于拥挤的问题，改用菜单原生快捷键栏。

### Shift S 状态

- Shift 松开时忽略 `Show keypresses for at least` 和渐暗设置，立即退出高亮。
- Shift 物理按住期间新按下的键独立保留 ShiftText；其他按键在 Shift 松开后立即回到普通文字。
- 保留 S 状态的按键将主文字稍微向右上移动，并在左下显示 `↑`，状态随该键自己的保持/渐暗生命周期结束。

### 当前验证

- Debug 构建通过：0 个警告、0 个错误。
- 布局探针确认缩放后的设置窗口中，原分组、新外观分组和底部按钮均位于客户区内且互不重叠。
- Win32 分层窗口测试确认背景 35%、键帽 75%、文字 100% 三种透明级别可独立生效。
- 实际 `Shift + ;` 输入测试确认 Shift 松开后仅 `;` 键保持白色 `:` S 状态及 `↑` 角标。
- 普通窗口与透明窗口均在 3000 ms 渐暗的后半段完成截图核对，只显示偏右上的 `:` 和左下 `↑`，未重新显现非 S 字符 `;`。
- 快捷切换测试确认状态会立即变化并写回配置。
- 当前仍需人工验证鼠标移入恢复、渐暗文字可读性和实际打字时的两种不透明模式。

## v1.4.0 - 2026-08-09

### 新增

- 在 `Settings` → `Input` 增加 `Fade keypresses` 复选框。
- 键盘键和鼠标键松开后，可在 `Show keypresses for at least` 设置的时长内线性淡回普通状态。
- 支持纯色按键样式和带背景图片的按键样式。
- 新增 `FadeKeyPresses` 全局配置并持久化保存；旧配置文件无需迁移，默认保持关闭。

### 行为与兼容性

- 渐暗仅在保持时间大于 `0 ms` 时可用。
- 按键按住期间保持完整高亮，松开后才开始渐暗。
- 关闭渐暗时，保持上游版本到达保持时间后立即取消高亮的行为。
- 组合键、Caps Lock/Shift 显示逻辑以及原有键盘布局格式保持兼容。

### 项目决策

- 暂不实现彩色按钮、彩虹循环或色相轮盘。
- 原因是颜色轮换不利于儿童准确分辨按键先后顺序；当前采用更简单、稳定的渐暗反馈。
- 相关早期设想已移入 `old` 目录，并明确标记为废稿。

### 验证

- 已完成简单人工功能测试。
- `dotnet build NohBoard\NohBoard.sln --configuration Debug --no-restore`：0 个警告，0 个错误。

## 上游更新

此前版本的更新记录请参阅 [ThoNohT/NohBoard Releases](https://github.com/ThoNohT/NohBoard/releases)。
